export const apiClient = {
    get: async (url: string) => {
        // Implementation
    },
    post: async (url: string, body: any) => {
        // Implementation
    }
};

export default apiClient;
